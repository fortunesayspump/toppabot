node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

build
# Large media — keep out of git (re-add via LFS/hosting later)
public/demo.mov
public/bgm.wav
